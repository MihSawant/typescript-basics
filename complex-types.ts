let lagnuages: string[];

lagnuages = ['Java', 'JavaScript', 'TypeScript', 'Python'];

// Default Type is any

let jsonObject;

jsonObject = {
    'name': 'Mihir',
    'age': 20,
    'dept': 'Development'
};

// Object Type Def of TS
let objectType: {
    name: string,
    age: number
};

objectType = {
    name: 'Mihir',
    age: 20
}
console.log(jsonObject);
console.log(objectType);
